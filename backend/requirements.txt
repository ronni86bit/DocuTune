# Runtime dependencies for the DocuTune FastAPI backend (see backend/Dockerfile).
# torch is installed separately from the CPU wheel index in the Dockerfile to
# keep the image lean; on GPU hosts install the CUDA build instead.
fastapi>=0.115,<1
uvicorn>=0.30,<1
pydantic>=2.7,<3
PyYAML>=6.0,<7
torch>=2.4,<3
transformers>=4.51,<6
accelerate>=1.0,<2
peft>=0.14,<0.22
huggingface_hub>=0.24
